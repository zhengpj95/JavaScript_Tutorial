/**
 * 线性表：由n个数据元素组成的有序序列。
 * 有长度限制且是有序的，
 * 在多个元素之间，第一个元素无前驱，最后一个元素无后继，中间元素有且进有一个前驱和一个后继
 *
 * 线性表有两种存储结构：
 * 1. 顺序结构存储，顺序表
 * 2. 链式结构存储，链表（又根据指针域的不同，分为单向链表，双向链表，循环链表等）
 */

/**
 * 顺序表
 */
class SequeceList {}

/**
 * 单向链表
 */
class SingleLinkedList {}

/**
 * 双向链表
 */
class DouleLinkedList {}

/**
 * 循环链表
 */
class CircleLinkedList {}
